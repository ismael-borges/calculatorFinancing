import { CalculatorFinancing } from './calculator-financing';

describe('CalculatorFinancing', () => {
  it('should create an instance', () => {
    expect(new CalculatorFinancing()).toBeTruthy();
  });
});
